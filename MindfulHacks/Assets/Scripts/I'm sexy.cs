using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Imsexy : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        cout << "Hello world" << endl;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
